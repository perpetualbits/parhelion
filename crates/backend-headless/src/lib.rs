//! Parhelion headless backend — in-memory rendering for deterministic tests.
//!
//! Governing doc: `docs/parhelion_milestone_plan.md` M0 (the headless backend
//! renders a test pattern to memory, feeding the golden-screenshot rig). The
//! backend catalogue lives in the subsystem table in `CLAUDE.md`; there is no
//! standalone backend design document yet.
//!
//! M0 status: empty skeleton. The in-memory renderer is a later M0 task with
//! its own prompt; this session creates only the crate.
